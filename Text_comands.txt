        push 5100
        in
        pop rbx
        pop rax
        push 0
        pop rcx

Circle:
        push 2
        push rcx
        rem
        push 1
        jne Colour:

        push 200
        push rcx
        div
        push 50
        sub
        push 200
        push rcx
        div
        push 50
        sub
        mul

        push 100
        push 2
        push rcx
        div
        rem
        push 50
        sub
        push 100
        push 2
        push rcx
        div
        rem
        push 50
        sub
        mul
        add

        push rbx
        push rbx
        mul
        push 32
        pop [rcx]
        jb Print:
        push 48
        pop [rcx]
        jmp Colour_cir:

Colour_cir:
        push 31

        push rcx
        push -1
        add
        pop rcx
        pop [rcx]
        push rcx
        push 1
        add
        pop rcx

        jmp Print:

Colour:
        push 29
        pop [rcx]

Print:
        push rcx
        push 1
        add
        pop rcx

        push 20000
        push rcx
        jb Circle:
        draw
        hlt
