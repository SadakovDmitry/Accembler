        push 5050
        in
        pop rbx
        pop rax
        push 0
        pop rcx

Circle:
        push 2
        push rcx
        rem
        push 1
        je Print:

        push 200
        push rcx
        div
        push 50
        sub
        push 200
        push rcx
        div
        push 50
        sub
        mul

        push 4
        push 200
        push rcx
        rem
        push 100
        sub
        push 200
        push rcx
        rem
        push 100
        sub
        mul
        div
        add

        push rbx
        push rbx
        mul
        push 46
        pop [rcx]
        jb Print:
        push 36
        pop [rcx]

Print:
        push rcx
        push 1
        add
        pop rcx

        push 20000
        push rcx
        jb Circle:
        hlt
